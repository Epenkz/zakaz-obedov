# Railway автоматически подставит DATABASE_URL при добавлении PostgreSQL.
# Локально для теста можно указать свою строку подключения:
# DATABASE_URL=postgresql://user:password@localhost:5432/zakaz_obedov
# PORT задаёт Railway автоматически. Локально можно 3000:
# PORT=3000
